-- local attackable = {}

-- local function not(event)
--     event.propagate = not
--         ( event.targets[1].attackableness == Attackableness.NO 
--           or event.targets[1].attackableness == Attackableness.SKIP )
-- end

-- attackable.not = function(entityClass)
--     retouch(entityClass, 'attack', )
-- end

-- return attackable
